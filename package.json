{
  "name": "drivn-reporting",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Drivn operator/route performance report generator (frontend). Reads all metrics from the central drivn-server; the browser never touches BigQuery.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.4"
  }
}

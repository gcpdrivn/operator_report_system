{
  "name": "operator-report-generator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Customizable per-operator bus performance report generator. Reads only the BigQuery MV layer through a read-only proxy; the browser never touches BigQuery.",
  "scripts": {
    "dev": "vite",
    "dev:server": "node --watch server/index.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.4"
  }
}
